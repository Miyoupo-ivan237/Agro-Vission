# Agro-Vission AI

Standalone diagnosis, crop recommendation, and local Ollama agronomist engines.

The public CommonJS API is exported from `index.js`. Feature modules are organized
under `diagnosis`, `recommendation`, and `agronomist`; compatibility implementations
remain at the project root while they are migrated incrementally.
